#include <stdio.h>

/**
 * main – It serves as the entry point for the program
 * Return: It returns zero by default if no errors otherwise it returns non-zero value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
